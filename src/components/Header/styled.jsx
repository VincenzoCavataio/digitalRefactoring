import styled from 'styled-components';

const LogoBanner = styled.img`
  position:relative;
  left: calc(50% - 156px);
  width: 250px;
  height: auto;
  margin: 30px;
  user-drag: none; 
`;

export default LogoBanner;