import styled from 'styled-components';

const LogoBanner = styled.img`
  position:relative;
  left: calc(50% - 206px);
  width: 360px;
  height: auto;
  margin: 30px;
  user-drag: none; 
`;

export default LogoBanner;